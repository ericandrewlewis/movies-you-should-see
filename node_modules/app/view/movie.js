var Backbone = require('backbone');
Backbone.$ = require('jquery');

module.exports = Backbone.View.extend({
	className: 'movie',

	render: function() {
		this.$el.html('<img src="' + this.model.posterUrl() + '">');
	}
});